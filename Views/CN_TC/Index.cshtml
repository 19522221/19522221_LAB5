@model IEnumerable<LAB5.Models.CN_TC>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<form asp-action="Select">
            <div class="form-group">
                <label class="control-label">Số triệu chứng</label>
                 < id = "name" input class="form-control" />
            </div>

            <div class="form-group">
               <button>< id = "butn"  type="submit" value="Liệt kê" class="btn btn-primary" /> </button> 
            </div>

      </form>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CongNhan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrieuChung)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CongNhan.MaCongNhan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrieuChung.MaTrieuChung)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
